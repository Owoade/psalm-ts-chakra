In your desktop directory run in the temina ` git clone https://github.com/Owoade/psalm-ts-chakra.git`
run `cd psalm-ts-chakra`
run `npm install`


Recreate the traversy task tracker UI ... just the UI
